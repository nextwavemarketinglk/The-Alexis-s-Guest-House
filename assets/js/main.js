const $=(s,c=document)=>c.querySelector(s), $$=(s,c=document)=>[...c.querySelectorAll(s)];
const header=$('.site-header'), menu=$('.main-nav'), toggle=$('.menu-toggle');
window.addEventListener('scroll',()=>header?.classList.toggle('scrolled',scrollY>40));
toggle?.addEventListener('click',()=>{const open=menu.classList.toggle('open');toggle.classList.toggle('open',open);document.body.classList.toggle('menu-open',open);toggle.setAttribute('aria-expanded',open)});
$$('.main-nav a').forEach(a=>a.addEventListener('click',()=>{menu.classList.remove('open');toggle?.classList.remove('open');document.body.classList.remove('menu-open')}));
const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('in')}),{threshold:.12});$$('.reveal').forEach(el=>io.observe(el));
const heroBg=$('.hero-bg');window.addEventListener('scroll',()=>{if(heroBg && innerWidth>760)heroBg.style.transform=`scale(1.05) translateY(${scrollY*.09}px)`});
const cursor=$('.cursor');if(cursor){window.addEventListener('pointermove',e=>{cursor.style.left=e.clientX+'px';cursor.style.top=e.clientY+'px'});$$('a,button,.tilt').forEach(el=>{el.addEventListener('mouseenter',()=>cursor.classList.add('hot'));el.addEventListener('mouseleave',()=>cursor.classList.remove('hot'))})}
$$('.tilt').forEach(card=>{card.addEventListener('pointermove',e=>{if(innerWidth<900)return;const r=card.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;card.style.transform=`perspective(1000px) rotateY(${x*3}deg) rotateX(${-y*3}deg)`});card.addEventListener('pointerleave',()=>card.style.transform='')});
const pad=n=>String(n).padStart(2,'0'), iso=d=>`${d.getFullYear()}-${pad(d.getMonth()+1)}-${pad(d.getDate())}`;
const ci=$('#checkin'),co=$('#checkout');if(ci&&co){const t=new Date(),a=new Date(t),b=new Date(t);a.setDate(t.getDate()+1);b.setDate(t.getDate()+2);ci.min=iso(t);ci.value=iso(a);co.min=iso(a);co.value=iso(b);ci.addEventListener('change',()=>{const d=new Date(ci.value+'T00:00:00');d.setDate(d.getDate()+1);co.min=iso(d);if(!co.value||co.value<co.min)co.value=co.min})}
function whatsappEnquiry(extra=''){const name=$('#name')?.value?.trim(),email=$('#email')?.value?.trim(),guests=$('#guests')?.value||$('#contactGuests')?.value,checkin=ci?.value||$('#contactCheckin')?.value,checkout=co?.value||$('#contactCheckout')?.value,message=$('#message')?.value?.trim();let parts=["Hello The Alexis's Guest House, I'd like to ask about availability."];if(name)parts.push(`Name: ${name}.`);if(checkin)parts.push(`Check-in: ${checkin}.`);if(checkout)parts.push(`Check-out: ${checkout}.`);if(guests)parts.push(`Guests: ${guests}.`);if(email)parts.push(`Email: ${email}.`);if(message)parts.push(`Message: ${message}`);if(extra)parts.push(extra);window.open('https://wa.me/94769103723?text='+encodeURIComponent(parts.join(' ')),'_blank','noopener')}
$('#enquireBtn')?.addEventListener('click',()=>whatsappEnquiry("Please send me today's rate."));$('#contactSubmit')?.addEventListener('click',e=>{e.preventDefault();whatsappEnquiry()});
$('#year') && ($('#year').textContent=new Date().getFullYear());
